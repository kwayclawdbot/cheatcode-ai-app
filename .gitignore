node_modules/
.env
.env.*
!.env.example
.expo/
dist/
.next/
proof/*.tmp
*.log
.DS_Store

# Agent worktrees (local only, contains node_modules)
.claude/

# Vercel CLI link (local only — the project is configured on Vercel itself)
.vercel/
