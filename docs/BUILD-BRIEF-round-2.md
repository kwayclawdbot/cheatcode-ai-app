# Cheat Code AI — Round 2 build brief (2026-08-26 late)

Owner picked, from the suggested list: **1 Setup detail · 2 Alerts · 3 Trade (symbol/search/watchlist) · 6 Debrief · 7 Community · 9 Account completion.** Skipped for now: 4/5 plans-orders-positions, 8 market-data worker, 10 deploy. Everything in `docs/BUILD-BRIEF-v1-slice.md` still binds (tokens, fonts, copy pattern, freshness, no gamification, one dominant action, volt=user / violet=Kai / cyan=market). Nav stays L6. Expo is **SDK 54** now — never bump it (`docs`: Expo Go 54.x ceiling).

## Lanes and file ownership (strict — commit only your paths; if git's index lock is busy, wait and retry)
| Lane | Owns | Must not touch |
|---|---|---|
| SCHEMA-2 | `supabase/migrations/0017+`, `supabase/seed.sql` (append only), `packages/shared/db.types.ts`, `docs/SCHEMA-NOTES.md` | apps/* |
| API-2 | `apps/api/**`, `packages/shared/api.ts` (append, keep existing exports), `apps/api/scripts/**` | supabase/, apps/mobile |
| MOBILE-A | `apps/mobile/src/app/setup/**`, `src/app/alert/**`, `src/app/(tabs)/alerts.tsx`, `src/app/(tabs)/trade.tsx`, `src/app/symbol/**`, `src/app/(tabs)/account.tsx`, `src/app/account/**`, `src/features/{setups,alerts,trade,account}/**`, and the SHARED files `src/lib/api.ts`, `src/lib/adapters.ts`, `src/lib/types.ts`, `src/lib/fixtures.ts`, `src/ui/**`, `src/app/(tabs)/_layout.tsx`, `src/app/_layout.tsx` | community/room/debrief routes |
| MOBILE-B | `src/app/(tabs)/community.tsx`, `src/app/room/**`, `src/app/debrief/**`, `src/app/contributor/**`, `src/features/{community,debrief}/**`, `src/lib/community-api.ts`, `src/lib/realtime.ts` | everything MOBILE-A owns. Need a new primitive? Create it under `src/features/community/ui/` — do NOT edit `src/ui/**`. Need a new API helper? Put it in `src/lib/community-api.ts`. |

Mobile lanes must not add npm dependencies except: MOBILE-A may add none; MOBILE-B may add none (supabase-js Realtime is already available). If a dependency is truly required, stop and report.

## Market data (API-2) — real, delayed, labeled
`POLYGON_API_KEY` is in `apps/api/.env.local` and works for REST aggregates. Implement `src/lib/market/polygon.ts`: daily + intraday (5m) aggregates and previous-close snapshot; cache candles into the `candles` table (timeframe '1d','5m'); snapshot cache in-memory 60s. Every quote → `{price, source_ts, received_ts, freshness}` with freshness computed by the 00 §5 thresholds **plus** a new value the review identified: when the plan/entitlement only permits delayed data, return `freshness:'delayed'` with `delay_reason:'entitlement'` rather than `stale` — the app must display it as delayed and NOT disable actions. No websockets this round. No holidays table still — session engine from ET clock + weekends, `holidays_known:false`.

**Seed setups must be coherent with real prices.** Add `apps/api/scripts/refresh-seed-setups.mjs`: for the 4 seed symbols, pull 30 daily candles from Polygon and recompute `entry_condition`/`stop`/`targets`/`invalidation`/`quote_snapshot` with a simple, documented rule (entry = break above the last 10-day high; stop = last 10-day swing low; first target = entry + 1.5 × (entry − stop); invalidation = daily close below stop), keep grade/score/state as seeded, set `score_components.seed=true, source:'polygon-daily', refreshed_at`, and rewrite `thesis_plain`/`thesis_technical` from a template with the real numbers. Run it once now and document it as the interim scanner until item 8 exists.

## API-2 endpoints (all `/api/v1`, auth'd, zod-validated, error envelope; add zod + types to `packages/shared/api.ts`)
Setups: `GET /setups/:id` (full object: live {quote, state, stepper, narration_plain, confirmations[]}, plan {entry, invalidation, stop, targets, size_suggestion from risk policy, scenarios[3], risk_reward}, learn {why_plain, evidence[] {label, ok:boolean}, similar_example|null, quiz {q, options[], answer_idx}|null}, explain {beginner,intermediate,advanced,family}, fit {ok, reasons[]}, next_action) · `POST /setups/:id/follow` (adds to Watching + drafts default ready-alert) · `GET /theses?symbol=&mode=`.
Alerts: `POST /alerts` `{draft_id}` → active (tier limit from entitlement_flags; error ENTITLEMENT_REQUIRED with upgrade info) · `GET /alerts/:id` (natural_language, structured `condition`, data_dependency, status history from user_events, originating refs resolved to labels) · `POST /alerts/:id/actions` `{action: pause|resume|cancel|edit, natural_language?}`. Alerts do not evaluate yet (no worker) — `monitoring:'armed_no_feed'` on each active alert so the UI can say "armed · live evaluation starts when market data goes live".
Trade: `GET /trade/landing?mode=` (02 §2 shape; `account_strip` = paper only; `markets.movers` from Polygon prev-close top movers of the seed universe; `kai_opportunities` = ranked setups) · `GET /trade/search?q=` (instruments by symbol/name prefix; if no instrument match, `intent:{kind:'kai_question', text}`) · `GET /symbols/:symbol?mode=` (02 §2 shape: quote header, chart config, mode lenses (the setup for that symbol per mode or a plain "no active setup" lens), kai_interpretation from the setup or a short cached Kai take (kai_objects, refs {symbol, market_date}), your_context {watchlisted, alerts[], plans:[]}, evidence {news: Polygon news v2 top 5 with published_utc}, community {thread_summary:null, sentiment:null} placeholder, actions[]) · `GET /market/candles?symbol=&tf=1d|5m&from=&to=` · `GET /market/snapshot?symbols=`.
Watchlist: `GET /watchlist`, `POST /watchlist {symbol}`, `DELETE /watchlist/:symbol` (thin wrappers over the SCHEMA-2 tables — client may also write direct under RLS; provide both).
Debrief: `GET /positions?status=closed` · `GET /debriefs` · `GET /debriefs/:id` · `POST /positions/:id/debrief` (Kai generates: outcome {pnl, held, exit_reason}, process_receipt[] {label, ok}, what_worked, what_failed, timeline[] from order_events/plan_events, lesson_plain; persisted via SCHEMA-2 `record_debrief` RPC; kai_object type `debrief`) · `POST /debriefs/:id/save-lesson` → writes `kai_user_memory` kind 'pattern' (requires memory_enabled) · **dev only** `POST /dev/simulate-closed-trade {symbol?}` gated by `DEV_TOOLS=1` → SCHEMA-2 `simulate_closed_trade` RPC creates plan → order (filled) → position (closed) → fills → user_events, all labeled `origin.simulated=true`; returns position id.
Community: `POST /rooms/:id/join` (core rooms only; via `join_core_room` RPC) · `GET /rooms/:id/messages?after_seq=&limit=` (via `messages_public` view; membership enforced) · `POST /rooms/:id/messages` `{kind:text|chart|position_update, body, refs?, structured_idea?, position_disclosure?}` (pipeline: zod → rate limit 10/min in-memory per user → spam precheck heuristics → disclosure required when structured_idea present → `post_room_message` RPC (assigns seq) → returns message) · `POST /rooms/:id/kai` `{command: summarize|verify|to_alert|compare|explain, args?, message_id?}` — **synchronous** in this round (no worker): run Kai with the room's last 50 messages as delimited untrusted content, produce the kai_object (room_summary | verification_card | alert_preview | comparison | briefing-as-explain), insert it as a `kai_object` message (user_id null) via RPC, return it · `POST /messages/:id/structured-assist` (Kai returns an improved draft; nothing published) · `POST /messages/:id/report {reason}` · `POST /rooms/:id/mute` / `unmute` (room_members.muted_until on self) · `GET /contributors/:user_id` (profiles_public + contributor_stats + role labels + position disclosures on recent messages; NO rankings).
Account: `GET /me` (profile, risk_policy, paper account, subscription tier, entitlement flags, memory_enabled) · `PUT /settings {explanation_level?, quiet_hours?, notification per_mode?, accessibility?{reduced_motion,text_scale}}` (accessibility stored in profiles.onboarding jsonb under `prefs` — note it) · `GET /memory` / `DELETE /memory/:id` / `DELETE /memory` / `PUT /memory/settings` · `POST /paper/reset` (via `reset_paper_account` RPC; once per month) · `GET /notifications?group=` (`action_required|changes|fyi`) + `POST /notifications/:id/read` · `POST /billing/checkout` → if `STRIPE_SECRET_KEY`+`STRIPE_PRICE_PREMIUM` set, create a Checkout Session (mode subscription, success/cancel deep links `cheatcodeai://billing/...`) else error `BILLING_NOT_CONFIGURED` with message_plain "Upgrades open soon." · `POST /webhooks/stripe` (verify signature; update `subscriptions`) — implement, untested if no key.
Notifications: create in-app `notifications` rows (kind, payload, deep link route) on: alert activated, Kai room reply to your @Kai, debrief ready, paper reset. `emitUserEvent` for every mutation.
Smoke: extend `scripts/smoke.sh` to cover every new endpoint (incl. a real `POST /rooms/:id/kai summarize` and a simulated trade → debrief). Keep it green.

## SCHEMA-2 (migrations 0017+, then `supabase db reset`, regen types, run `scripts/rls-test.mjs` + extend it for watchlists)
- `0017_watchlists`: `watchlists(id, user_id, name default 'Watchlist', position int)` + `watchlist_items(watchlist_id, symbol references instruments, added_at, note text, pk(watchlist_id,symbol))`; RLS owner select/insert/update/delete (client-direct allowed per 00 §4); trigger: default watchlist created on profile insert.
- `0018_rpcs`: `join_core_room(p_user_id, p_room_id)` (core rooms only, idempotent) · `post_room_message(p_user_id, p_room_id, p_kind, p_body, p_refs, p_structured_idea, p_position_disclosure, p_parent_id)` (member + not muted/banned + room not restricted unless moderator; assigns `seq` via a `room_seq_counters` table row lock; inserts; returns row) · `post_kai_message(p_room_id, p_kai_object_id, p_body)` (user_id null, kind kai_object) · `record_debrief(p_user_id, p_position_id, p_outcome, p_process_review, p_kai_summary, p_kai_object_id)` (insert debriefs + user_events atomically) · `reset_paper_account(p_user_id)` (once per calendar month: cash/buying_power/equity ← starting_balance, close nothing, reset_count+1, user_events) · `simulate_closed_trade(p_user_id, p_symbol, p_entry, p_exit, p_qty)` (trade_plan status closed → order filled + order_events + fills → position closed with realized_pnl → user_events; `origin.simulated=true` everywhere) · `set_room_mute(p_user_id, p_room_id, p_until)` · `notify(p_user_id, p_kind, p_payload)` (insert notifications, channel in_app).
- Grants: execute to service_role only (and to authenticated for `join_core_room`, `set_room_mute` if you make them SECURITY DEFINER with auth.uid() checks — your call, document it).
- Append `docs/SCHEMA-NOTES.md` with interpretations.

## MOBILE-A screens (artboards in `design/artboards/`; existing primitives in `src/ui/`; keep fixtures mode working for every new screen)
1. **Setup detail** `src/app/setup/[id].tsx` — from `Setup-detail.html` (palette file) + `V3-K1-Research.html`: sticky identity (ticker, grade badge, state dot+label, freshness), in-object view switch **Live / Plan / Learn** (segmented, volt active), Live: quote + mini chart (MiniChart with real 5m/1d candles from `/market/candles`) + stepper + narration + confirmations list; Plan: Entry/Target/Invalid tiles, size suggestion, scenarios, R/R, primary "Watch it" (→ follow → alert draft) and secondary "Build plan" DISABLED with hint "plans arrive with paper trading"; Learn: why_plain, evidence checklist, level selector chips (Beginner/Intermediate/Advanced/Family) swapping `explain`, quiz (one question, reveals answer, no score/XP), "Ask Kai about this" (→ Home composer with pinned setup: navigate to home with `?ask=` param and pinned setup id — Home already creates conversations; add the pinned param handling in `src/lib/useKai.ts`). "Why?" on Home opens this screen on the Learn view.
2. **Alerts** — rewrite `(tabs)/alerts.tsx` on real `GET /alerts`: Needs Attention / Watching / Active Trades (empty w/ copy) / Triggered / History; draft rows have "Activate" (→ `POST /alerts`), active rows show "armed · evaluation starts when market data goes live" from `monitoring`; `src/app/alert/[id].tsx` (S41 wireframe on canvas: plain condition, expandable structured logic, data dependency, history, originating trace chips, actions pause/resume/cancel/edit, ONE primary); `src/app/alert/new.tsx` NL builder sheet ("Watch META for a break above 504" → `POST /alerts/draft` → preview of structured condition → Activate). Entry points: setup detail "Watch it", symbol page "Set alert", Alerts "+".
3. **Trade** — rewrite `(tabs)/trade.tsx` on `GET /trade/landing` (V4-TR1: PAPER strip, search, Continue (drafted alerts / followed setups), Kai opportunities, watchlist with real snapshot prices + freshness, movers, catalysts row omitted if empty), search screen `src/app/symbol/search.tsx` (instrument results + "Ask Kai: …" intent row), **symbol detail** `src/app/symbol/[symbol].tsx` from `V4-TR2-Symbol-detail.html`: quote header w/ freshness + bid/ask omitted (no L1 feed) , timeframe chips 1D/5D/1M/3M/YTD/1Y driving `/market/candles`, chart with the setup's Entry/Target/Invalid lines when a setup exists (cyan/green/red semantic), mode lens chips, Kai interpretation card, "Your context" (watchlisted, alerts), Evidence (news list with published time), Community block placeholder text "Discussion opens with Community rooms" (B ships rooms; link to `/room/[id]` if a setup room exists — read `setups.discussion_room_id`), actions: Ask Kai / Set alert / Add to watchlist / **no Buy/Sell** (paper orders are a later round; show a disabled "Buy · Sell — paper trading arrives next" pair so the artboard's geometry holds honestly).
4. **Account** — rewrite `(tabs)/account.tsx` on `GET /me` + sub-screens under `src/app/account/`: `settings.tsx` (explanation level, quiet hours, reduced motion, text scale), `notifications.tsx` (S72: grouped inbox with deep links; mark read), `memory.tsx` (list, delete one, delete all, master switch), `paper.tsx` (balance, reset once/month with confirm), `subscription.tsx` (tier, what premium adds from entitlement_flags, "Upgrade" → checkout URL in browser or the BILLING_NOT_CONFIGURED sheet), `debriefs` row → `/debrief` list (route owned by B; just link), dev-only "Simulate a closed paper trade (dev)" button shown when `EXPO_PUBLIC_DEV_TOOLS=1`, sign out. Broker: "None — add a broker (later release)".
Wire the alerts tab badge to real needs_attention count. Keep every price with FreshnessMark; `delay_reason:'entitlement'` renders as "Delayed 15m" not stale.

## MOBILE-B screens
1. **Community** — rewrite `(tabs)/community.tsx` (V3-C0): mode selector chip, "Setup rooms" (rooms of type setup for the current mode's setups, with discussing counts from message counts), "Rooms" core list with active counts (members) — tapping joins (`POST /rooms/:id/join`) and opens `src/app/room/[id].tsx`. NO live block (Phase 2) — replace the artboard's LIVE card with a quiet "Live sessions arrive in a later release" line, not a fake.
2. **Room** `src/app/room/[id].tsx` from `V3-C1` + S81 wireframe: header (name, member count, slow mode if config), pinned intelligence (room.pinned + the setup object if setup room), message list paged by seq with Realtime subscription on `room:{id}` (Supabase Realtime `postgres_changes` on messages filtered by room_id — verify RLS lets the member receive it; else poll every 5s and note it), member messages with role labels + "Holds X" disclosure chips, kai_object messages rendered as objects (room_summary / verification_card / alert_preview / comparison), catch-up pill ("N new since you left" from room_members.last_read_seq), composer with `@Kai` chip that opens a command sheet (Summarize since open · Verify this claim (needs a selected message) · Mark levels · Turn into alert · Compare bull vs bear · Explain to a beginner) → `POST /rooms/:id/kai`, plus "Post an idea" → structured composer.
3. **Structured composer** `src/app/room/[id]/compose.tsx` (S84): Direction & thesis / Entry condition / Invalidation / Risk & size / Target & horizon / Evidence; "Ask Kai to review" → `POST /messages/:id/structured-assist` shows the improved draft with Accept/Keep mine — nothing posts without the explicit Post; position disclosure toggle required for structured ideas.
4. **Contributor profile** `src/app/contributor/[userId].tsx` (S85): role labels, contribution history counts, usefulness/clarity, disclosures on recent posts, Follow (local only — no follows table; show as "Save" to a local list and note), Mute / Report. No rankings anywhere.
5. **Debrief** `src/app/debrief/index.tsx` (list of the user's debriefs + closed positions without one → "Get Kai's debrief" → `POST /positions/:id/debrief`) and `src/app/debrief/[id].tsx` from `V3-T2` (outcome number w/ semantic colour, "exited in target zone · held 2h 14m", process receipt checklist, Kai's lesson panel, what worked / what failed, timeline replay strip, "Chart replay" DISABLED with hint, "Save lesson" → `/debriefs/:id/save-lesson` then shows "Saved to what Kai remembers"). Simulated trades render a small "SIMULATED" tag (from origin.simulated).

## Verification gate (each lane, hard)
- API-2: `npm run build` clean; `scripts/smoke.sh` green incl. new endpoints; refresh-seed-setups run once and `GET /home` still coherent.
- SCHEMA-2: `supabase db reset` clean; rls-test green (extended).
- MOBILE lanes: `npx tsc --noEmit` clean; `npx expo export --platform web` clean; extend `scripts/proof.mjs` (fixtures) with every new screen at 390×844 into `proof/`; run `scripts/proof-live.mjs` unchanged to prove no regression; add a `proof-live-2.mjs` per lane that drives your new screens against the real stack (Metro on :8081 is running — do NOT restart it; API on :3000; Supabase local) and saves `live2-*.png`. Look at your own screenshots before finishing; fix what's wrong.
- Commit only your paths with a clear message. Final message = what's real vs fixture, proof list, deviations from artboards and why, blocked items.
